import React from 'react';
import './Button.css';

const CancelButton = () => {
    return (
        <div>
            <button className='viewButton'> Cancel </button>
        </div>
    );
};

export default CancelButton;